围绕一个c语言ai辅助教学平台构建，你了解下，看看是否能搭建。
可以申请软著，外加这一篇普刊论文

既支持c语言，还可像coding park luna那样进行checkpoint引导提问的平台

## Luna-for-C 自建方案

参考 ITiCSE 2025 Prompt Programming + Coding Park Theia 架构：
前端：Theia / Monaco 编辑器（接 C 语言服务）
执行：Judge0 / 容器 gcc，编译+valgrind 测段错误
AI：校内私有化 Qwen-Coder / DeepSeek-Coder，系统 prompt 写死 Socratic 规则
checkpoint：教师配「关卡 JSON」——每关有引导问题+正则/单元测试过关条件，
不过关则编辑器遮罩下一关代码区（前端硬锁）
日志：prompt/AI 回复/代码 diff/过关时间戳 全表留存

### 一、参考原型与资料链接

1. Coding Park Web IDE / Luna​
   Socratic 教练在工作区观测阻塞、checkpoint 验证理解、教师端全量时间线；基于 Eclipse Theia​
   https://www.codingpark.io/en/web-ide
2. ITiCSE 2025 Prompt Programming（Padurean et al.）​
   在奥克兰大学 1031 人《C 程序设计》实测：多轮对话+按需执行+多函数题，GPT-4o-mini 做 Socratic 助教，限流防依赖
   arXiv:2503.04267 / ACM DOI 10.1145/3724363.3729094
3. Eclipse Theia 架构​
   前后端 JSON-RPC、Monaco 编辑器、LSP、PTY 终端，可嵌自定义扩展做"工作区观测+硬锁"
   https://deepwiki.com/eclipse-theia/theia/1.3-architecture-overview
4. Judge0 CE​
   开源沙箱判题，C 用 gcc 编译，返回 stdout/stderr/信号级错误（SIGSEGV 等），支持多文件、自定义 flags、时间内存限制
   https://compiler.otomeyt.ai/ （API 文档） / github judge0/judge0
5. CodeHelp / Focused Tutor 思路​
   Socratic 门控对话流（答不到点不推进），可借鉴其 prompt 工程
   codehelp.app（前文提过）

### 二、整体架构（Luna-for-C）

┌──────────────────────────────────────────────────────────┐
│ 浏览器（学生端） │
│ Eclipse Theia 定制前端 │
│ ├─ Monaco 编辑器（C 语法高亮，禁粘贴 AI 输出可选） │
│ ├─ 终端 xterm.js → 后端 PTY（可 gcc 本地试，但判题走Judge0）│
│ ├─ Luna 教练侧栏（Socratic 对话） │
│ └─ Checkpoint 遮罩层（下一关代码区只读，过关才解禁） │
└───────────────┬──────────────────────────────────────────┘
│ WebSocket JSON-RPC
┌───────────────┴──────────────────────────────────────────┐
│ 后端 Node.js（Theia backend + 自研 Edu-Extension） │
│ ├─ 文件服务 / Git 提交钩子（每次保存=一次 commit，留痕） │
│ ├─ Checkpoint 引擎：读关卡 JSON，验测试/正则/AI 评分 │
│ ├─ AI 网关：校内私有化 Qwen-Coder / DeepSeek-Coder │
│ │ system prompt 写死：只反问不给完整函数、C 指针题必问边界│
│ └─ 日志服务：写 PostgreSQL（见第四节字段） │
└───────────────┬──────────────────────────────────────────┘
│ REST
┌───────────────┴──────────────────────────────────────────┐
│ Judge0 CE（Docker 沙箱，独立部署） │
│ gcc -std=c11 -Wall -Wextra 编译 → 跑隐藏测试 → valgrind │
│ 返回：stdout / stderr / signal(SIGSEGV=8) / time / memory │
└──────────────────────────────────────────────────────────┘

### 三、Checkpoint 硬门控怎么实现（Luna 灵魂）

Luna 式 checkpoint 不是"AI 聊天里劝你"，而是编辑器层硬锁。实现三件套：

1. 关卡描述（教师配的 JSON）
   {
   "task_id": "fib_L2",
   "checkpoints": [
   {
   "id": "cp1",
   "guide_question": "递归终止条件是什么？n<0 时返回什么？",
   "gate_type": "ai_socratic",
   "pass_rule": "AI 判学生自然语言回答含 'n<=1' 且不含完整 fib 代码",
   "unlock": "editor_region:lines 12-20"
   },
   {
   "id": "cp2",
   "guide_question": "写 fib(10000) 普通递归会怎样？",
   "gate_type": "test_pass",
   "test": "judge0_run(fib_tail.c) && stdout=='55\\n'",
   "unlock": "editor_region:lines 22-40"
   }
   ]
   }
2. 前端硬锁（Theia 扩展里）
   用 Theia 的 EditorWidget 装饰器把"未解锁行"设为 readOnly + 灰显
   学生过 cp1 前，第 12–20 行看不见也不能点
   过 cp2 前，尾递归区锁死——这就是 Luna 在 Python 端做的事，C 端用同样扩展机制
3. 后端判定流
   学生点"请求验证"
   → 后端收当前文件+回答文本
   → 若 ai_socratic：送 AI 网关按 rubr
   ic 判（不吐代码只判答到点没）
   → 若 test_pass：抽代码交 Judge0 跑隐藏用例
   → 过则发 WebSocket 事件 → 前端解锁对应 region
   → 全过 → 允许 Hand in
   关键点：AI 只判"引导回答是否到点"，不负责编译；编译永远走 Judge0 真 gcc。这避开 LLM 幻觉判 C 代码的坑。

### 四、AI 交互日志表（对应 ACM 2026 要求）

PostgreSQL 表 ai_interaction_log 最小字段：
字段 :
id : 主键
student_id : 学号
task_id : 题号
checkpoint_id : 对应 cp1/cp2
ts : 时间戳
role : user/assistant
prompt_text : 学生问句或回答
ai_reply : Luna 回复（只引导不代码）
code_before : 提交前编辑器文本（Git blob 引用）
code_after : 验证后文本
gate_result : passed / failed / escalated
model : qwen-coder/ds-coder + version
tokens : 用量
教师端仪表板直接 SELECT * FROM ai_interaction_log WHERE student_id=? ORDER BY ts 回放时间线——这就是 CodeGrade AI 日志的平替，但带 checkpoint 上下文。

### 五、C 语言专属 Socratic 规则（system prompt 片段）

你是一名 C 语言教练 Luna-C。规则：

1. 学生问"给我 fib 代码"，你必须反问："先告诉我递归终止条件怎么定？n 为负怎么办？"
2. 涉及指针题，必问"这块内存谁分配谁释放？free 几次？"
3. 学生贴段代码问"为什么段错误"，你不得直接改代码，
   必须问："用 printf 在哪一行的前后打印地址，能定位到哪一步 NULL？"
4. 严禁输出完整函数体；可输出伪代码或单行示意。
5. 若学生连续 3 次答非所问，标记 blocker 上报教师端。

### 六、学期落地节奏（滨海学院可写进申报书）

第 1 阶段（1 个月）：Theia 定制前端 + Judge0 CE 单机部署，跑通"禁 AI 闭卷 C 题 + 自动判题"
第 2 阶段（2 个月）：加 Luna-C 侧栏（接校内 Qwen-Coder），做 ai_socratic 关卡，选《程序设计基础》1 个班 30 人试点斐波那契/链表逆置
第 3 阶段（申硕年）：补 checkpoint 硬锁前端扩展 + 教师日志大盘 + 导出 CSV，申报"边缘智能与嵌入式系统"方向的教学信息化平台资产

### 七、风险点（报告里主动讲，显得专业）

C 编译型语言没有"运行时行为观测"，Luna 在 Python 能看变量变更，C 只能看"编译错/段错误信号"，Socratic 触发点比 Python 粗——需用"引导提问+隐藏测试"双轨补。
Theia 定制前端工作量不小（约 1 名全栈 2–3 月），纯教师 solo 做不动，需信息中心配合或本科生创新项目承接。
AI 判自然语言回答仍需 rubric 工程，粗判可用正则+关键词，细判需少量人工抽检（可让研究生助管教 Rubric）。
