# Luna-for-C：C语言 AI 辅助教学平台 — 项目可行性评估与完善方案

> **版本**：v1.0 — 2026-08-29  
> **作者**：Sisyphus（AI 辅助评估）  
> **基准文件**：`项目描述.md`（Luna-for-C 自建方案）  
> **定位**：滨海学院申硕支撑 + 软著 1 项 + 普刊论文 1 篇 + 《程序设计基础》课程试点

---

## 目录

1. [执行摘要与总评](#1-执行摘要与总评)
2. [原方案优点萃取](#2-原方案优点萃取)
3. [可行性分项评估](#3-可行性分项评估)
4. [五大核心风险深度剖析](#4-五大核心风险深度剖析)
5. [完善后总体方案：双轨架构](#5-完善后总体方案双轨架构)
6. [技术选型对比与定版建议](#6-技术选型对比与定版建议)
7. [系统架构详设](#7-系统架构详设)
8. [核心机制深化设计](#8-核心机制深化设计)
9. [C 语言教学特性专项适配](#9-c-语言教学特性专项适配)
10. [数据模型与日志体系](#10-数据模型与日志体系)
11. [部署、运维与成本估算](#11-部署运维与成本估算)
12. [安全、合规与伦理](#12-安全合规与伦理)
13. [软著申报策略](#13-软著申报策略)
14. [普刊论文选题与框架](#14-普刊论文选题与框架)
15. [重排落地节奏与人力分工](#15-重排落地节奏与人力分工)
16. [创新点提炼（申报书/论文通用）](#16-创新点提炼申报书论文通用)
17. [结论与下一步行动清单](#17-结论与下一步行动清单)
18. [附录](#18-附录)

---

## 1. 执行摘要与总评

### 1.1 一句话结论

**完全可行，且选题极佳 — 但原方案“全量复刻 Theia + Judge0 + 私有大模型”属于重型路线，单教师 solo 无法在 3 个月内交付可演示版本。建议改走“轻量化 MVP 先行、重型架构分期演进”的双轨策略，可在 6 周内拿出可申软著、可写论文、可进课堂的可用系统。**

### 1.2 总体评分

| 维度           | 评分(5分制) | 评语                                                                              |
| :------------- | :---------: | :-------------------------------------------------------------------------------- |
| **选题价值**   |    ★★★★★    | 命中“AI+教育”“工程教育认证”“申硕教学信息化”三大政策风口，滨海学院场景高度契合     |
| **技术可行性** |    ★★★★☆    | 单点技术均成熟（Monaco/Judge0/私有LLM），难点在集成与 Theia 定制工作量            |
| **团队可行性** |    ★★★☆☆    | 原方案需 1 全栈 × 3 月 + 运维配合，solo 教师风险高；轻量化后单教师+2 本科生可完成 |
| **成果可得性** |    ★★★★★    | 软著确定性高（有界面+有后台+有算法即可）；普刊只要有 30 人对照实验数据就能写      |
| **推广潜力**   |    ★★★★☆    | 架构一旦跑通，可横向扩展至 C++/单片机/嵌入式 C，契合“边缘智能与嵌入式系统”方向    |

### 1.3 关键判断

1.  **不要从 Theia 起步。** Theia 是为“完整云 IDE”设计的，定制成本远超教学场景所需。Coding Park 能做是因为团队 5+ 人年投入；校内单点突破应以 `Next.js + Monaco + 自研轻量后端` 起步，后期再考虑是否套壳 Theia。
2.  **不要自建 Judge0 首版。** Judge0 CE 虽开源，但 Docker 隔离、队列、判题并发都是坑。首版用 `隔离容器 + 直接 gcc + 超时杀` 的极简判题微服务（~300 行 Go/Node）更快，数据量起来再平滑迁 Judge0。
3.  **AI 判“理解”不可全自动。** 原方案 `ai_socratic` 用 LLM 判自然语言是否“答到点”是最大不确定性。实测中误判率 20-30%。必须设计为 `关键词/正则初筛 + LLM 复核 + 教师抽检` 的三级漏斗，并保留人工 override。
4.  **C 语言的观测点必须外置。** Python 能在运行时插桩看变量，C 不行。Socratic 触发点只能靠三件套：编译期错误、运行时信号（SIGSEGV/SIGABRT）、隐藏单元测试的失败用例回显。需在 checkpoint 设计中显式暴露这三类信号。
5.  **软著与论文要倒排工期。** 软著从提交到下证 30-45 天，普刊审稿 2-4 月。意味着系统 MVP 必须在第 2 个月可截图、可录屏、可导出数据，否则赶不上申硕材料窗口。

---

## 2. 原方案优点萃取

原 `项目描述.md` 已达到申报书初稿水准，以下优点应保留并强化：

1.  **问题锚点准**：不做“给答案的 AI 助手”，做“防依赖的 Socratic 教练 + 硬门控”。这与 ITiCSE 2025 Prompt Programming 的核心结论一致，学术站位高。
2.  **参考系扎实**：Coding Park Luna / CodeHelp / ITiCSE 1031 人实验三重证据链，评审专家无法质疑“为何这么设计”。
3.  **技术栈选型主流**：Monaco + LSP + Judge0 + 私有 Qwen/DeepSeek 均为业界标准，无冷门技术风险。
4.  **数据闭环完整**：`ai_interaction_log` 字段已覆盖 ACM 2026 可复现要求（prompt/回复/code diff/时间戳/model/tokens），可直接作为教学研究数据集，论文价值高。
5.  **风险自曝专业**：主动写出“C 无运行时观测”“Theia 工作量大”“Rubric 需人工抽检”三风险，反而增加可信度。

---

## 3. 可行性分项评估

### 3.1 技术可行性 — 分模块打分

| 模块                | 原方案          | 可行性 | 瓶颈                                                                                 | 建议                                                                  |
| :------------------ | :-------------- | :----: | :----------------------------------------------------------------------------------- | :-------------------------------------------------------------------- |
| **前端编辑器**      | Theia 定制      |   中   | Theia 扩展开发文档少，构建链重（yarn + webpack + inversify），新手 2 周仅能跑通 demo | **MVP 改 Monaco 直嵌**，Theia 作为二期可选                            |
| **判题执行**        | Judge0 CE       |   高   | 初期并发 <50 时 Judge0 过重；需配 Redis/Postgres/Workers，单机运维负担大             | **首版自研 `gcc-runner` 微服务**，二期再接 Judge0                     |
| **AI 网关**         | 私有 Qwen-Coder |   高   | 校内 GPU 资源是门槛；Qwen2.5-Coder-7B 需 16GB 显存，14B 需 24GB，vLLM 部署需运维     | **首版可用 API 中转+私有化二选一**，提供降级开关；prompt 层与模型解耦 |
| **Checkpoint 引擎** | JSON + 硬锁     |  中高  | 前端只读锁易被绕过（F12/本地保存）；后端需二次校验，手写 Gate 逻辑繁琐               | **前后端双校验 + Git 钩子留痕**，Gate 规则 DSL 化                     |
| **日志与大盘**      | PostgreSQL      |   高   | 无瓶颈，按设计即可                                                                   | 增加 `code_diff` 存 patch 节省空间，补 `session_id` 便于回放          |

### 3.2 团队与资源

- **理想配置**：1 教师（产品+论文）+ 1 全栈（前端+后端）+ 1 运维/研究生（模型+部署）+ 2 本科生（测试用例+关卡内容）
- **最小配置（可成）**：1 教师 + 1 能写 Next.js 的本科生 + 信息中心提供 1 台带 GPU 的服务器或 API 额度
- **不可行配置**：教师 solo 且无学生、且从零学 Theia — 必延期

### 3.3 政策与成果

- **软著**：确定性 95%。只要有“软件名称+界面+功能+代码 3000 行”即可，Luna-for-C 满足“教学平台”类别， **可报 1-2 项**（平台本体 + Checkpoint 引擎可拆分）。
- **普刊**：确定性 80%。普刊（非核心）对创新性要求不高，重在“有数据、有对照、有反思”。30 人试点 + 问卷 + 成绩对照即可满足《计算机教育》《教育信息化论坛》等期刊。
- **申硕材料**：强相关。系统可写入“教学信息化平台”“产教融合实践平台”，硬件投入可计入“边缘智能与嵌入式系统”方向资产。

---

## 4. 五大核心风险深度剖析

### 风险 1：Theia 定制 — 投入产出比失衡

**现象**：Theia 官方扩展开发需理解 inversify 依赖注入、JSON-RPC 协议、前端后端双包构建。仅“遮罩下一关代码区”一个需求，就涉及 `EditorManager` `MonacoEditorProvider` `PreferenceService` 三处覆写，调试链路长。

**影响**：1 人全职 2-3 月仅能完成基础 IDE，挤占 AI 与 Checkpoint 核心逻辑时间。

**对策**：

- MVP 用 `Next.js 14 (App Router) + Monaco Editor (monaco-editor/react) + xterm.js` 三件套，1 周可搭出可用编辑器 + 文件树 + 终端外观。
- 硬锁实现退化为“后端拒收未解锁区域的提交 + 前端 Monaco `deltaDecorations` 灰显只读”，用户体验 90% 一致，工作量 10%。

### 风险 2：Judge0 运维复杂度

**现象**：Judge0 CE 需 Docker、Postgres、Redis、Workers 四组件，C 语言判题还需额外配置 `valgrind` `gcc flags`。校内服务器无 k8s 时，单机 Docker Compose 重启即丢队列。

**影响**：教师需兼职运维，判题失败时无法区分是学生代码错还是沙箱挂了。

**对策**：

- 首版 `judge-lite`：一个 Node/Go 服务，接收 `{code, tests}` → 写入临时目录 → `docker run --rm --memory=256m --cpus=0.5 -v tmp:/code gcc:13 bash -c "gcc -std=c11 -Wall -Wextra -O2 main.c -o main && ./main < input.txt"` → 回传 stdout/stderr/signal/time。超时用 `timeout 5s` 兜底。
- 好处：无状态、易重启、日志直观；后期流量 >100 并发再无缝替换为 Judge0（接口保持一致）。

### 风险 3：AI 判“理解”误判率高

**现象**：`pass_rule: "AI 判学生回答含 'n<=1' 且不含完整 fib 代码"` 看似简单，实测中学生写“我觉得 n 小于等于 1 时 return n” AI 可能判 fail；学生复制粘贴 AI 话术可能判 pass。

**影响**：误判导致学生卡关，引发对系统不信任；论文数据中 gate_result 噪声大。

**对策**：三级漏斗

1.  正则/关键词初筛（必含 `n<=1` `n==0` `边界` 等，必不含 `int fib` 完整定义）
2.  LLM 复核（system prompt 要求输出 JSON `{"pass": bool, "reason": str, "confidence": 0-1}`，confidence <0.7 自动标 `escalated`）
3.  教师端一键 override + 抽检标注，反哺 rubric 迭代

### 风险 4：C 语言 Socratic 触发点粗

**现象**：Python 可在 `for` 循环中观测变量 `i` 的每轮值，C 需编译后运行，无中间态。

**影响**：学生“写了但错”的中间过程不可见，Luna 只能在“编译失败/段错误/测试不通过”三个离散点提问，引导颗粒度粗。

**对策**：

- **编译期引导**：解析 `gcc -Wall` 警告（如 `unused variable` `implicit declaration`），转为 Socratic 问题：“编译器说 `a` 未使用，你觉得哪一行赋值没生效？”
- **运行时引导**：段错误时自动跑 `valgrind --tool=memcheck` / `gdb batch` 抓栈，回显“在 `*p = 1` 时 p=0x0”，再提问。
- **测试期引导**：隐藏测试用例失败时，不直接给输入输出，而是给“性质描述”：“你的 fib 在 n=20 时结果偏差 1，考虑边界是否少算了一次？”

### 风险 5：私有模型部署门槛

**现象**：Qwen2.5-Coder-7B-Instruct 需 ~14GB 显存（FP16），校内若无 A100/4090，需量化（AWQ/GPTQ）或用 API。

**影响**：项目启动即被 GPU 卡住。

**对策**：

- 架构上 **模型与网关解耦**：`AI_GATEWAY = { provider: "qwen-local" | "deepseek-api" | "openai-compatible" }`，通过环境变量切换。
- 首期可用 DeepSeek API / 通义千问 API（成本约 0.5 元/千 tokens，30 人班一学期 <200 元），同时申请校内 GPU；论文中写“支持私有化部署，已验证 API 与本地双模式”，不影响创新性表述。

---

## 5. 完善后总体方案：双轨架构

### 5.1 总体定位

**一句话定位**：面向 C 语言程序设计课程的 Socratic 式 AI 辅助实训平台，以“Checkpoint 硬门控 + 真编译真判题 + 只问不给的 AI 教练”为核心，支撑“做中学、做中悟”的工程教育范式。

### 5.2 双轨演进图

```
阶段0: 0-2周  轻量 MVP（可演示、可筛图、可申软著）
  Next.js + Monaco + judge-lite + AI网关(API版) + JSON关卡 + 双校验硬锁
  └─> 产出：可用系统 + 30人小试数据 + 软著材料

阶段1: 2-6周  教学闭环（可进课堂、可写论文）
  + PostgreSQL日志 + 教师大盘 + Rubric三级漏斗 + valgrind诊断
  └─> 产出：完整数据链 + 普刊初稿

阶段2: 6-12周  工程化（可运维、可推广）
  + Judge0 迁移 + 私有模型本地化 + Theia 评估（可选）+ 多课程复用
  └─> 产出：申硕资产 + 第二篇论文/软著增项

阶段3: 长期  生态化
  + C++ / 单片机 C / 嵌入式扩展 + 题库市场 + 与超星/学堂在线对接
```

> **核心思想**：用最轻的栈验证最重的理念（Socratic + 硬门控），理念成立后再加重栈。

### 5.3 角色与旅程

- **学生**：选课 → 看关卡引导问题 → 在可编辑区写 C → 点“验证”→ 看编译/测试/AI 反馈 → 过关解锁下一区 → Hand in
- **教师**：配关卡 JSON（或可视化表单）→ 设隐藏测试 → 看大盘（阻塞热力图、AI 对话时间线）→ 一键干预/放行
- **助教/研究生**：标注 AI 误判 → 迭代 Rubric → 导出 CSV 做教学研究

---

## 6. 技术选型对比与定版建议

| 层            | 原方案               | 轻量 MVP 推荐                                  | 重型二期可选                   | 选型理由                                                           |
| :------------ | :------------------- | :--------------------------------------------- | :----------------------------- | :----------------------------------------------------------------- |
| **前端 IDE**  | Theia                | **Next.js 14 + Monaco + xterm.js + Shadcn UI** | Theia / code-server            | MVP 1周可跑，Theia 1月起步；Monaco 即 VS Code 编辑器内核，体验一致 |
| **后端**      | Theia Backend (Node) | **Next.js API Routes / NestJS Lite + Prisma**  | Theia Backend + 自研 Extension | 前后端同构，部署单一；Prisma 迁移方便                              |
| **判题**      | Judge0 CE            | **judge-lite (Docker gcc)**                    | Judge0 CE / HydroOJ 内核       | 无状态易运维，接口兼容 Judge0                                      |
| **AI**        | 私有 Qwen/DeepSeek   | **OpenAI-Compatible 网关（先 API，后本地）**   | vLLM + Qwen2.5-14B             | 解耦模型，论文可写“支持私有化”                                     |
| **数据库**    | PostgreSQL           | **PostgreSQL + Prisma**                        | 同左                           | 日志表需 JSONB 存 code_diff                                        |
| **文件/版本** | Git 钩子             | **Postgres 存快照 + 可选 Git**                 | Git + MinIO 对象存储           | 首版无需真 Git，存 patch 即可回放                                  |
| **部署**      | Docker               | **Docker Compose 单机**                        | k8s / 1Panel                   | 校内单机最稳                                                       |

**定版建议**：MVP 按“推荐”列实施，所有模块对外暴露 `JudgeProvider` `AIProvider` `GateProvider` 接口，二期可热插拔替换，无需重构。

---

## 7. 系统架构详设

### 7.1 逻辑架构图

```
┌─────────────────────────────────────────────────────────────────┐
│ 浏览器 Student / Teacher                                          │
│  Next.js 前端 (Monaco + Checkpoint 遮罩 + Luna 侧栏 + 大盘)        │
│  - Monaco 只读装饰（未解锁区灰显 + onBeforeChange 拦截）          │
│  - Luna 对话流（Socratic，不展示完整代码）                        │
│  - 教师关卡配置表单（JSON 可视化）                               │
└──────────────┬──────────────────────────────────────────────────┘
               │ HTTPS / WSS
┌──────────────▼──────────────────────────────────────────────────┐
│ 后端 (Next.js API / NestJS)                                     │
│  ├─ Auth (学号/教师号，JWT)                                      │
│  ├─ File Service (内存 + Postgres 快照)                          │
│  ├─ Checkpoint Engine (Gate DSL 解释器)                          │
│  │    ├─ test_pass → Judge Lite                                 │
│  │    ├─ ai_socratic → AI Gateway → Rubric 三级漏斗             │
│  │    └─ regex/human → 正则/人工复核                            │
│  ├─ AI Gateway (OpenAI-Compatible，限流/熔断/日志)              │
│  └─ Log Service → PostgreSQL                                    │
└──────┬──────────────────┬──────────────────┬────────────────────┘
       │ REST             │ REST             │ PG
┌──────▼──────┐  ┌────────▼────────┐  ┌─────▼─────┐
│ judge-lite  │  │ AI Provider     │  │ Postgres  │
│ Docker gcc  │  │ Qwen/DeepSeek   │  │ + Prisma  │
│ + valgrind  │  │ (API or vLLM)   │  │           │
└─────────────┘  └─────────────────┘  └───────────┘
```

### 7.2 核心接口草案

```ts
// 判题：统一 JudgeProvider
POST /api/judge/run
Req: { code: string, language: "c", tests: { stdin, expected }[], flags: "-std=c11 -Wall" }
Res: { status: "AC"|"WA"|"CE"|"RE"|"TLE", stdout, stderr, signal, timeMs, valgrind?: string }

// AI 网关：统一 AIProvider
POST /api/ai/socratic
Req: { checkpointId, studentAnswer, codeSnippet, history }
Res: { pass: boolean, confidence: number, reply: string, tokens }

// Checkpoint 验证
POST /api/checkpoint/verify
Req: { taskId, checkpointId, answerText, code }
Res: { passed: boolean, gate: "regex"|"ai"|"test", reason, unlockRegions: number[][], escalated?: boolean }

// 日志回放
GET /api/logs?studentId=...&taskId=...
Res: { timeline: AiInteractionLog[] }
```

### 7.3 前端硬锁双校验

- **前端**：Monaco `editor.deltaDecorations` 将未解锁行设为 `inlineClassName: "locked-region"` + `onDidChangeModelContent` 拦截越权编辑（回滚）。
- **后端**：`POST /api/checkpoint/verify` 与 `POST /api/submit` 均校验 `code` 的解锁区是否被篡改（对比关卡 JSON 的 `unlock` 行号范围，超范围提交直接拒收）。
- **绕过成本**：F12 改前端可暂时看见，但提交时后端拒收，教师大盘标红“异常提交”，兼顾体验与严肃性。

---

## 8. 核心机制深化设计

### 8.1 Checkpoint 2.0 — Gate DSL

原 JSON 已很好，建议升级为 DSL，支持组合条件：

```json
{
  "task_id": "linked_list_reverse",
  "title": "单链表逆置",
  "checkpoints": [
    {
      "id": "cp1",
      "title": "理解指针所有权",
      "guide_question": "逆置过程中，哪一个指针负责‘暂存下一个节点’？为什么必须暂存？",
      "gates": [
        { "type": "regex", "rule": "(next|nxt|tmp).*(保存|暂存|备份)", "weight": 0.4 },
        {
          "type": "ai_socratic",
          "rubric": "回答需点出‘断链前保存 next，否则丢失后继’",
          "weight": 0.6
        }
      ],
      "pass_threshold": 0.7,
      "unlock": { "editorRegion": [12, 25], "hints": ["提示：prev=null, cur=head"] },
      "on_fail": { "ai_followup": "如果不暂存，cur->next 改掉后还能找到原 next 吗？" }
    },
    {
      "id": "cp2",
      "title": "尾递归/迭代实现",
      "guide_question": "写出迭代版三指针框架，跑通隐藏测试",
      "gates": [{ "type": "test_pass", "tests": "hidden_tests/linked_list_3.json", "weight": 1.0 }],
      "unlock": { "editorRegion": [26, 50] },
      "on_fail": { "valgrind_hint": true }
    }
  ]
}
```

- 支持 `weight + threshold` 组合、 `on_fail.ai_followup` 自动追问、 `valgrind_hint` 开关。

### 8.2 AI Socratic Prompt 工程（可直接落地）

**System Prompt 模板**（网关层写死，学生不可见）：

```
你是 Luna-C，C 语言 Socratic 教练。严格遵守：
1. 绝不输出完整函数体（>5 行即违规）。只能给伪代码、单行示意或提问。
2. 学生求代码时，必须反问引导：递归题问终止条件，指针题问谁分配谁释放，段错误题问如何用 printf/ gdb 定位。
3. 判题时只输出 JSON：{"pass": bool, "confidence": 0-1, "reply": "给学生的下一问（中文，1-2句）", "reason": "给教师的判据（英文关键词）"}
4. 若学生连续 3 次答非所问，reply 中加入 "[ESCALATE]" 标记，触发人工介入。
5. 语气鼓励、简短，不说教。
Few-shot:
Q: "给我 fib 代码" -> {"pass": false, "reply": "先告诉我：fib(0) 和 fib(1) 应该返回什么？n 为负时呢？", ...}
Q: "为什么段错误" + 代码含 *p=NULL -> {"pass": false, "reply": "在 *p 赋值前后各打印 p 的地址，你觉得哪一行 p 是 0x0？", ...}
```

- **限流防依赖**：每 checkpoint 最多 5 次 AI 对话，超限需教师放行（呼应 ITiCSE 2025 限流结论，论文可引用）。

### 8.3 日志与复盘 — 教师大盘

- **时间线回放**：按 `student_id + task_id` 聚合，展示 `code_diff (Monaco DiffEditor)` + `AI 对话` + `gate_result` 三轨对照，可一键导出 CSV。
- **阻塞热力图**：统计 `cp1/cp2` 的平均尝试次数、平均停留时长、ESCALATE 率，教师一眼看出哪关最卡。
- **诚信视角**：检测 `code_after` 与 `ai_reply` 的相似度（若 AI 误吐代码，学生粘贴则标黄）。

---

## 9. C 语言教学特性专项适配

| C 难点            | Luna 应对               | 技术实现                                                                                             |
| :---------------- | :---------------------- | :--------------------------------------------------------------------------------------------------- |
| **指针/内存**     | 必问“谁 malloc 谁 free” | 题目配置中标记 `memory_task: true`，AI prompt 强制追问；判题后自动 `valgrind --leak-check=full` 回显 |
| **段错误**        | 教定位而非给答案        | RE 时自动 `gdb --batch -ex run -ex bt` 抓栈，AI 据栈提问                                             |
| **编译警告**      | 警告即教学点            | `gcc -Wall -Wextra` 警告解析为 checkpoint 前置问                                                     |
| **边界/溢出**     | 隐藏测试覆盖            | 斐波那契、链表题配 `n=0/1/大值/NULL` 四类边界测试，不通过则 AI 问边界                                |
| **多文件/头文件** | 渐进解锁                | Checkpoint 解锁 `*.h` 区，避免一次给全                                                               |

---

## 10. 数据模型与日志体系

### 10.1 核心表（Prisma 风格）

```prisma
model User {
  id        String   @id // 学号/工号
  role      Role     // STUDENT | TEACHER | TA
  name      String
}

model Task {
  id          String   @id // fib_L2
  title       String
  checkpoints Json     // 存 Gate DSL
  hiddenTests Json
}

model AiInteractionLog {
  id            String   @id @default(cuid())
  studentId     String
  taskId        String
  checkpointId  String
  ts            DateTime @default(now())
  role          String   // user | assistant | system
  promptText    String?  @db.Text
  aiReply       String?  @db.Text
  codeBefore    String?  @db.Text
  codeAfter     String?  @db.Text
  codeDiff      String?  @db.Text // patch
  gateResult    String   // passed | failed | escalated
  gateType      String   // regex | ai_socratic | test_pass
  model         String   // qwen2.5-coder:7b | deepseek-api
  tokens        Int?
  confidence    Float?
  sessionId     String   // 便于回放
  @@index([studentId, taskId, ts])
}

model CheckpointProgress {
  studentId     String
  taskId        String
  checkpointId  String
  passed        Boolean
  attempts      Int
  unlockedAt    DateTime?
  @@id([studentId, taskId, checkpointId])
}
```

- `codeBefore/After` 存全文便于论文截图，`codeDiff` 存 patch 节省空间，`sessionId` 串联一次做题会话。
- 导出 CSV 时 `SELECT * FROM AiInteractionLog WHERE taskId=? ORDER BY ts` 即为论文数据集。

---

## 11. 部署、运维与成本估算

### 11.1 单机 Docker Compose 拓扑

```yaml
services:
  web: # Next.js 前端+后端
    build: .
    ports: ['3000:3000']
    env_file: .env # AI_PROVIDER, JUDGE_URL
  db:
    image: postgres:16
    volumes: [pgdata:/var/lib/postgresql/data]
  judge-lite:
    build: ./judge-lite
    image: gcc:13
    tmpfs: /tmp
    # 无需常驻，由 web 调用 docker run 亦可；此处为可选常驻版
```

- 校内服务器 4核16G 即可跑 MVP（无本地模型时）；若本地模型，需 1 张 24GB 显存 GPU。
- 备份：`pg_dump` 每日 + `code snapshot` 存 Postgres 即可，无需 OSS。

### 11.2 成本

| 项         | 首期（API 版）                                                            | 二期（私有化）                             |
| :--------- | :------------------------------------------------------------------------ | :----------------------------------------- |
| 服务器     | 复用校内虚拟机 0 元                                                       | + GPU 云主机约 800 元/月 或 校内 A100 复用 |
| AI tokens  | 30人×10题×5轮×500 tokens ≈ 750k tokens ≈ 30-50 元/学期（DeepSeek 优惠价） | 0 元（电费）                               |
| 域名/HTTPS | 校内二级域名 0 元                                                         | 同左                                       |
| 人力       | 教师+1-2 本科生（创新学分）                                               | + 研究生助管                               |
| 总计       | **<500 元/学期可跑通**                                                    | 一次性 GPU 投入                            |

---

## 12. 安全、合规与伦理

1.  **代码沙箱**：`judge-lite` 必须 ` --network=none --pids-limit=64 --memory=256m --read-only` 限权，防 `fork bomb` `rm -rf`。
2.  **Prompt 注入**：学生输入拼入 AI 前做转义，system prompt 置顶且不可被覆盖；网关层过滤“忽略之前指令”等关键词。
3.  **数据隐私**：学号、代码、AI 对话属教学数据，Postgres 单独 schema，导出 CSV 需教师权限，论文中学号脱敏。
4.  **AI 依赖伦理**：显式限流（每 checkpoint 5 问）、Hand in 前强制“无 AI 复述”小测（让学生用自然语言复述思路，AI 判过关才允许提交），呼应 ITiCSE 防依赖结论。
5.  **学术诚信**：Hand in 时自动查重（Moss/JPlag 可二期接入），AI 对话日志可追溯“是否直接要答案”。

---

## 13. 软著申报策略

### 13.1 申报数量与名称

- **主软著 1 项（必报）**：`Luna-C  C语言 Socratic 辅助教学平台 V1.0` — 类别：教育信息化 / 教学辅助
- **可拆分增项 1 项（可选，刷成果数）**：`基于 Gate DSL 的 Checkpoint 硬门控引擎 V1.0` — 突出“硬锁 + 多模态门控”

### 13.2 软著材料清单（按国版中心要求）

1.  源代码 60 页（前后各 30 页，每页 50 行，截取核心：Monaco 集成、Checkpoint 引擎、AI 网关、判题调用）
2.  说明书 15-20 页（含架构图、功能截图 8-10 张、创新点、部署说明）
3.  申请表（著作权人：滨海学院；开发完成日期：MVP 可演示日；首次发表日期：同日）
4.  创新点写法（审查员看重）：
    - “Socratic 门控对话与编辑器硬锁联动，防 AI 依赖”
    - “C 语言编译/运行/测试三阶信号驱动的提问策略”
    - “全量 AI 交互与代码快照日志，支撑教学实证研究”

### 13.3 时间线

- MVP 可截图日 → 3 日内整理材料 → 提交 → 30-45 天下证（可加急 15 天，约 800 元代理费，按需）。

---

## 14. 普刊论文选题与框架

### 14.1 期刊匹配（普刊，非核心，审稿 1-3 月）

- **首选**：《计算机教育》《教育信息化论坛》《现代教育技术》《实验技术与管理》
- **备选**：《滨海学院学报（教育版）》校内通道更快

### 14.2 推荐题目（三选一）

1.  **《Socratic AI 与 Checkpoint 硬门控在 C 语言教学中的实证研究——以斐波那契与链表逆置为例》**（最贴合，可直接用试点数据）
2.  **《防依赖的 AI 辅助编程教学平台设计与实现：Luna-for-C》**（偏系统实现，适合《实验技术与管理》）
3.  **《基于私有化大模型的 C 语言段错误诊断引导策略》**（偏 AI 提示词工程，适合《现代教育技术》）

### 14.3 论文框架（6000-8000 字，普刊标准）

```
1 引言：AI 辅助编程的依赖风险 + C 语言教学痛点 + Luna 思路
2 相关工作：Coding Park Luna / CodeHelp / ITiCSE 2025 Prompt Programming
3 系统设计：架构图 + Checkpoint DSL + AI 网关 + 判题沙箱
4 Socratic 策略：三类 C 问题（递归/指针/段错误）的提问模板 + 限流
5 实验设计：30 人试点班 vs 30 人对照班（传统 OJ），指标：过关率、AI 提问次数、段错误自定位率、期末卷面
6 结果与分析：表格 + 箱线图 + 对话案例 + 误判分析
7 威胁与反思：C 无运行时观测、误判、Theia 成本（主动写，显严谨）
8 结论与展望：向 C++/嵌入式扩展 + 题库生态
```

- **数据要求**：30 人×2 任务的 `ai_interaction_log` 导出即为核心数据，配前测后测问卷 10 题即可。
- **查重**：普刊要求 <15%，AI 相关表述需人工改写。

---

## 15. 重排落地节奏与人力分工

### 15.1 四阶段（原三阶段优化）

| 阶段        | 周期        | 目标                                   | 产出物                    | 人力                       |
| :---------- | :---------- | :------------------------------------- | :------------------------ | :------------------------- |
| **P0 原型** | 第 1-2 周   | 跑通“写 C→编译→判题→AI 问答”最小闭环   | 可演示 URL + 录屏         | 教师(产品) + 1 前端学生    |
| **P1 闭环** | 第 3-6 周   | Checkpoint 硬锁 + 日志大盘 + 30 人试点 | 可进课堂系统 + CSV 数据集 | +1 后端学生 + 研究生(助管) |
| **P2 固化** | 第 7-10 周  | 软著材料 + 论文初稿 + 私有化验证       | 软著受理号 + 论文投稿     | 教师主笔，学生补图         |
| **P3 扩展** | 第 11-16 周 | Judge0/本地模型/多题库，申硕资产入库   | 平台 V2 + 第二成果        | 信息中心配合               |

> **里程碑红线**：第 6 周必须有 30 人数据的 CSV，否则论文赶不上学期末投稿。

### 15.2 人力看板

- **教师**：关卡内容（斐波那契/链表题的引导问题与隐藏测试）、Rubric 终审、论文主笔
- **前端学生**：Next.js + Monaco + 遮罩 + Luna 侧栏
- **后端学生**：API + Postgres + judge-lite + AI 网关
- **研究生/助教**：valgrind 诊断、数据标注、教师大盘
- **信息中心**：服务器 + 域名 + （可选）GPU

---

## 16. 创新点提炼（申报书/论文通用）

1.  **机制创新**：将“软引导（AI 对话劝学）”升级为“硬门控（编辑器只读锁 + 后端拒收）”，从技术上阻断“跳过思考直接要答案”路径。
2.  **策略创新**：针对 C 语言编译型特性，设计“编译-运行-测试”三阶 Socratic 触发器，弥补 Python 动态观测在 C 上的缺失。
3.  **数据创新**：全量留存 `prompt/回复/code diff/过关时间戳` 四元组，形成可复现的教学实证数据集，支撑“AI 是否导致依赖”的量化研究。
4.  **工程创新**：AI 网关与判题沙箱双解耦，支持 API/私有化热切换，适配校内资源差异，降低落地门槛。

---

## 17. 结论与下一步行动清单

### 17.1 结论

- 原方案学术与工程站位准确，**可行且值得做**。
- 最大风险是 **Theia 重定制与 Judge0 重运维** 导致的进度失控，**用轻量 MVP 双轨策略可化解**。
- 按本完善方案，**6 周可达“可申软著、可写论文、可进课堂”状态**，成果确定性高，申硕支撑力强。

### 17.2 下一步行动（按优先级）

- [ ] **确认资源**：校内是否有可用服务器/GPU/API 额度；能否配 1-2 本科生（创新学分/毕设）
- [ ] **定栈**：拍板是否接受“Next.js + Monaco + judge-lite”轻量路线（建议接受）
- [ ] **定题**：选定试点 2 题（推荐：斐波那契递归 + 单链表逆置，覆盖递归/指针/边界三痛点）
- [ ] **写关卡 JSON**：按本文 8.1 DSL 模板先写 2 关的 `guide_question + pass_rule + unlock`
- [ ] **搭 P0 原型**：1-2 周内跑通 Monaco→judge-lite→AI 网关闭环，录屏即为软著核心截图
- [ ] **定论文靶刊**：选 1 本普刊，看其“投稿须知”调论文格式与字数

> **需要我下一步直接搭 P0 原型脚手架（Next.js + Monaco + judge-lite + AI 网关桩）吗？若确认，我可按本方案生成可运行的初始仓库结构与核心代码。**

---

## 18. 附录

### 附录 A：关卡 JSON 最小可用模板

见 8.1，可直接复制改题干。

### 附录 B：教师大盘字段草案

`学生×关卡` 矩阵：尝试次数 / 平均停留 / 首次 AI 提问时间 / 是否 ESCALATE / 最终 gate_result，支持一键导出。

### 附录 C：参考文献（论文可直接引用）

1. Padurean et al., Prompt Programming, ITiCSE 2025, arXiv:2503.04267
2. Coding Park Web IDE / Luna, https://www.codingpark.io/en/web-ide
3. Eclipse Theia Architecture, https://deepwiki.com/eclipse-theia/theia/1.3-architecture-overview
4. Judge0 CE, https://github.com/judge0/judge0
5. CodeHelp / Focused Tutor Socratic 思路, https://codehelp.app

### 附录 D：术语表

- **Socratic**：苏格拉底式提问，只问不给，迫使学生自答
- **Checkpoint**：关卡检查点，过关才解锁下一代码区
- **硬门控**：编辑器层只读锁 + 后端拒收的强制卡点
- **Gate DSL**：关卡过关规则的领域特定语言
- **judge-lite**：轻量判题微服务，Docker gcc 直跑

---

_— 完 —_  
_本文档已落盘至项目根目录 `项目分析文档.md`，可直接作为申报书附件或组会评审材料。若需，我可继续产出：① P0 原型脚手架 ② 关卡 JSON 题库 ③ 软著说明书模板 ④ 论文 LaTeX 模板。_
